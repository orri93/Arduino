#include "range.h"
#include "macro.h"

namespace gatl = ::gos::atl;

namespace gos {
namespace temperature {
namespace range {

Real setpoint(SETPOINT_MININUM, SETPOINT_MAXIMUM);
Unsigned output(PID_MINIMUM_OUTPUT, PID_MAXIMUM_OUTPUT);

} // namespace range
} // namespace temperature
} // namespace gos
